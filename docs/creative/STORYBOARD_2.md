# Storyboard 2.0

`Storyboard2` is a contiguous timed shot grammar. Each `Shot` has canonical `shot_id`/`scene_id`, order, timing, purpose, visual source, product state/feature, composition, camera/motion, text, transitions, audio, capture instruction, keyboard policy, and QC requirements. IDs are unique and downstream references are rejected when not in the canonical set.
